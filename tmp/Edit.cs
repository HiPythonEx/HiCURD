
        private bool OnEdit(DataGridView dgvMain, int index, #table#VO vo)
        {
            if (vo != null)
            {
                //string id = DGViewUtil.GetCellValue(dgvMain, index, 1);
                //vo.User_id = id;
				throw new NotImplementedException("this function not support in rest");
            }
            return false;
        }
